# Copy this file to .env.local and fill in real values. Do NOT put real keys here.
# .env.local is gitignored; .env.example is committed as documentation.

# Sorcery code-review API key (used only by a local helper script, never in the browser)
SORCERY_API_KEY=
